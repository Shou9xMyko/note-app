/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import React from "react";

const NoteItemDate = ({ formatedDate }) => {
  return <p className="note-item__date">{formatedDate}</p>;
};

export default NoteItemDate;

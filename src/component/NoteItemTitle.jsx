/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */
import React from "react";

const NoteItemTitle = ({ title }) => {
  return <h3 className="note-item__title">{title}</h3>;
};

export default NoteItemTitle;
